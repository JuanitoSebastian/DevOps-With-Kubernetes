apiVersion: batch/v1
kind: CronJob
metadata:
  name: create-wiki-todo
  namespace: project
spec:
  schedule: "0 * * * *"
  jobTemplate:
    spec:
      backoffLimit: 2
      template:
        spec:
          containers:
            - name: create-wiki-todo
              image: create-wiki-todo-job
              imagePullPolicy: IfNotPresent
              envFrom:
                - configMapRef:
                    name: todo-db-config
                - secretRef:
                    name: todo-db-secret
          restartPolicy: Never