apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todo-image-claim
spec:
  storageClassName: todo-image-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi